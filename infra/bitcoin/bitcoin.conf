# Ativa rede de testes local isolada (não interage com testnet/mainnet).
regtest=1
# Habilita modo servidor para aceitar chamadas JSON-RPC.
server=1
# Em cenários reais, manter P2P habilitado ajuda sincronização e resiliência.
listen=1
# Indexa transações para permitir consultas históricas mais amplas.
txindex=1

# CIDRs autorizados a chamar RPC (rede interna / Docker).
# Aqui permitimos apenas faixas privadas típicas de rede interna em containers.
rpcallowip=10.0.0.0/8
rpcallowip=172.16.0.0/12
rpcallowip=192.168.0.0/16
# Credenciais RPC de exemplo (alinhar com .env / .env.example do backend).
rpcuser=bitcoinrpc
rpcpassword=bitcoinrpcdevpassword

# Publisher ZMQ de hash de bloco (repasse leve e direto para apps).
zmqpubhashblock=tcp://0.0.0.0:28332
# Publisher ZMQ de hash de transação (repasse leve e direto para apps).
zmqpubhashtx=tcp://0.0.0.0:28333

# Configurações específicas da rede regtest devem ficar nesta seção.
[regtest]
# Interface de bind do RPC (0.0.0.0 = todas interfaces do container).
# Com a porta não publicada no host, fica acessível apenas na rede Docker privada.
rpcbind=0.0.0.0
